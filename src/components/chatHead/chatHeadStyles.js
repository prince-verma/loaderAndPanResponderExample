import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  chatHead: {
    height: 80,
    width: 80,
    borderRadius: 40,
    backgroundColor: '#000',
    position: 'absolute',
    top:10,
    left:2,
  }
})
